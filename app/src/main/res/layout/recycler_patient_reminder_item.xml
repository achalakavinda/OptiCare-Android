<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorFontPanelAlpha"
    android:layout_marginBottom="2dp"
    android:layout_marginTop="2dp"
    android:id="@+id/EventItemCard"
    android:orientation="horizontal">

    <ImageView
        android:layout_width="0dp"
        android:layout_weight="0.1"
        android:layout_height="50dp"
        android:src="@drawable/ic"/>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_weight="0.8"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="5dp">

        <TextView
            android:id="@+id/time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Time 18:10:00"
            android:paddingBottom="10dp"/>

        <TextView
            android:id="@+id/description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="You can use layout_weight properties. If you want to take 70% of parent width you must set child layout_width property value 0dp, like android:layout_width="/>

    </LinearLayout>

    <ImageButton
        android:id="@+id/deleteBtn"
        android:src="@drawable/ic_delete_forever_black_24dp"
        android:layout_width="0dp"
        android:layout_weight="0.1"
        android:layout_height="wrap_content"/>

</LinearLayout>